set $mod Mod4
set $term urxvtcd
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
for_window [title="FTerm"] floating enable, resize grow up 300, resize grow down 300, resize grow left 75
bindsym $mod+Return exec --no-startup-id $term
bindsym $mod+Shift+Return exec --no-startup-id $term -title "FTerm"
bindsym $mod+Alt+Return split h, exec --no-startup-id $term
bindsym $mod+m exec --no-startup-id $term
bindsym $mod+Shift+m exec --no-startup-id $term -title "FTerm" 

## kill focused window
bindsym $mod+w kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run -i -p ">" -fn "-xos4-terminus-medium-r-*-*-14-*" -b -nf white -sb gray -sf black
# rofi for window switching
bindsym $mod+g exec --no-startup-id rofi -show window -modi window -location 7 -width 100 -lines 5

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right
bindsym $mod+c focus output left

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right
bindsym $mod+Shift+c move output left

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+q floating toggle

# change focus between tiling / floating windows
bindsym $mod+q focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+a focus child

# switch to workspace number
bindsym $mod+1 workspace number 1I
bindsym $mod+2 workspace number 2II
bindsym $mod+3 workspace number 3III
bindsym $mod+4 workspace number 4IV
bindsym $mod+5 workspace number 5V
bindsym $mod+6 workspace number 6VI
bindsym $mod+7 workspace number 7VII
bindsym $mod+8 workspace number 8VIII
bindsym $mod+9 workspace number 9IX
bindsym $mod+0 workspace number 10X

#go back to previous workspace pressing $mod+NUM again
workspace_auto_back_and_forth yes
# move focused container to workspace number
bindsym $mod+Shift+1 move container to workspace number 1I
bindsym $mod+Shift+2 move container to workspace number 2II
bindsym $mod+Shift+3 move container to workspace number 3III
bindsym $mod+Shift+4 move container to workspace number 4IV
bindsym $mod+Shift+5 move container to workspace number 5V
bindsym $mod+Shift+6 move container to workspace number 6VI
bindsym $mod+Shift+7 move container to workspace number 7VII
bindsym $mod+Shift+8 move container to workspace number 8VIII
bindsym $mod+Shift+9 move container to workspace number 9IX
bindsym $mod+Shift+0 move container to workspace number 10X

# reload the configuration file
bindsym $mod+Alt+r reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec --no-startup-id "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

#toggle master window(it just gets bigger tho)
bindsym $mod+space resize grow height 10 px or 10 ppt, resize grow width 10 px or 10 ppt
bindsym $mod+Shift+space resize grow height -10 px or -10 ppt, resize grow width -10 px or -10 ppt


bar {  
    output HDMI1
    tray_output HDMI1
    strip_workspace_numbers yes
    status_command i3status -c ~/.config/i3/i3status.conf
    #status_command ~/.i3/status_test.py
    #status_command ~/.i3/layout_status.sh
    #mode hide
    #hidden_state hide
    #modifier Mod5
    position bottom
    font xft:Terminus 8
    #font pango: Ubuntu Mono 9

colors {  
        background #151515
        #statusline #D7D0C7
        statusline #ffffff
        separator #404040
        # colors           border  bg      text
        focused_workspace  #d3d7cf #babdb6 #3a3a3a
        inactive_workspace #888a85 #151515 #D7D0C7
        urgent_workspace   #ff0000 #151515 #D7D0C7
        #urgent_workspace   #888a85 #ff7400 #D7D0C7
    }
}
# SECONDARY MONITOR
bar {  
    output VGA1
    tray_output none
    strip_workspace_numbers yes
    position top
    font xft:Terminus 8

colors {  
        background #151515
        statusline #ffffff
        separator #404040
        # colors           border  bg      text
        focused_workspace  #d3d7cf #babdb6 #3a3a3a
        inactive_workspace #888a85 #151515 #D7D0C7
        urgent_workspace   #ff0000 #151515 #D7D0C7
        #urgent_workspace   #888a85 #ff7400 #D7D0C7
    }
}
#2 px border for new windows
new_window pixel 2
new_float pixel 2

#show/hide border on mod+/ and mod+?
bindsym $mod+slash exec --no-startup-id i3-msg border normal
bindsym $mod+Shift+slash exec --no-startup-id i3-msg border pixel 2
#wallpaper
exec --no-startup-id feh --bg-center ~/Pictures/wallhaven-1167.jpg
#second monitor
exec --no-startup-id xrandr --output VGA1 --auto --left-of HDMI1
#focused tile color
# class                 border  backgr. text    indicator 
#default: client.focused          #4c7899 #285577 #ffffff #2e9ef4
client.focused          #000000 #FF3300 #ffffff #282828
client.focused_inactive #333333 #2d2d2d #ffffff #484e50
#ff3300
#sound
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q set Master 2%+ unmute
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q set Master 2%- unmute
bindsym XF86AudioMute exec amixer -q set Master toggle
# screenlock
bindsym $mod+Scroll_Lock exec --no-startup-id i3lock -c 111111 -d

font pango: DejaVu Sans Mono 10
hide_edge_borders vertical
#assign firefox to workspace#2
assign [class="Firefox"] 2II
for_window [class="Firefox"] border none, layout tabbed
# imagemagic
for_window [title="ImageMagick.*"] border none, floating enable
#assign skype to workspace#3
#focus_follows_mouse yes|no
focus_follows_mouse no
#force_focus_wrapping yes|no
#workspace_auto_back_and_forth yes|no
#screenshot
bindsym Print exec --no-startup-id scrot ~/Pictures/screenshots/$(date +%F_%H%M%S_%N).png
bindsym --release Shift+Print exec --no-startup-id scrot -s ~/Pictures/screenshots/$(date +%F_%H%M%S_%N).png
#reboot, shutdown and shit menu
set $mode_system System (e) logout, (s) suspend, (h) hibernate, (r) reboot, (p) power off
mode "$mode_system" {
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id systemctl suspend, mode "default"
    bindsym h exec --no-startup-id systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym p exec --no-startup-id systemctl poweroff, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Pause mode "$mode_system"

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# i3-input on Mode+i
bindsym $Mod+i exec --no-startup-id i3-input
